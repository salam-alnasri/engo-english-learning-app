import 'package:flutter/material.dart';

class A6 extends StatelessWidget {
  const A6({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('A6 Screen')),
      body: Center(child: Text('Content for A6 Screen')),
    );
  }
}
